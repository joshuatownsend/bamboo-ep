{
  "name": "bamboo-ep",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "probe": "pnpm --filter @bamboo-ep/core probe"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
